{
  "name": "spell-parser",
  "version": "1.0.0",
  "description": "Parses a csv for an Alexa skill",
  "main": "csvFetch.js",
  "scripts": {
    "fetch": "node csvFetch.js",
    "parse": "ruby parse.rb",
    "updateLocal": "npm run fetch && npm run parse",
    "deploy": "echo \"Need to build script to deploy to Dynamo\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^7.0.0",
    "lodash": "^4.17.11"
  }
}
